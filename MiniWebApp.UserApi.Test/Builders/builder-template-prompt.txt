C# Expert Prompt: Deferred Execution Builder Generator
Role: Act as a Senior C# Software Architect.

Task: Generate a specialized "Builder" pattern implementation for a Domain Model or DTO using Deferred Execution via System.Lazy<T>.

1. ARCHITECTURAL STRUCTURE
Class Definition: Create a public partial class [EntityName]Builder.

Inheritance: Inherit from Builder<[EntityName], [EntityName]Builder>.

Backing Fields: Use private Lazy<T> _[propertyName] for every property to ensure values are only evaluated upon calling .Build().

Constructor: Initialize all Lazy fields with sane default values (e.g., new(() => "Default Value") or new(() => default!) for required strings).

2. BOILERPLATE METHODS (REQUIRED)
Build() Override: Return a new instance of [EntityName], mapping all _field.Value properties to the entity's properties.

Fluent Setters: * With[Property](T value): Reassigns the Lazy field with the provided value.

Without[Property](): Reassigns the Lazy field to default! or null.

Cloning: Include WithValuesFrom([EntityName] example) to map an existing object's properties into new Lazy wrappers.

3. OBJECT MOTHER LOGIC (CUSTOM EXTENSIONS)
WithRandom[Property](): For identifying strings (Name, Email, SKU, etc.), use the base class RandomString() method.

WithDefaults() Override: Implement this to chain various With... methods, ensuring all required/non-nullable fields are populated with valid, randomized, or placeholder data.

4. CONSTRAINTS & SYNTAX
Use C# 11/12 syntax (target-typed new, file-scoped namespaces).

Strictly adhere to Nullable Reference Types (use string? vs string based on the input model).

Ensure all fluent methods return Instance (from the base class) or this to maintain the chain.

ENTITY TO MODEL: [INSERT YOUR CLASS DEFINITION OR LIST OF PROPERTIES HERE]